// frontend/config.js
const BASE_URL = "https://event-projrct-1.onrender.com"; // Ensure this matches your backend server
// You'd need to explicitly import/use this BASE_URL in your fetch calls if you want to use it
// For now, hardcoded "http://localhost:3000" is used directly in HTML scripts.